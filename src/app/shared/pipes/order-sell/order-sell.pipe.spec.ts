import { OrderSellPipe } from './order-sell.pipe';

describe('OrderSellPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderSellPipe();
    expect(pipe).toBeTruthy();
  });
});
